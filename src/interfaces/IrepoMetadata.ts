interface RepoMetadata {
    name: string;
    url: string;
    publicUrl?: string;
    thumbnail: string;
    info: string;
    title: string;
    customConfig?: Object;
}

export default RepoMetadata;
